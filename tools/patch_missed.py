# -*- coding: utf-8 -*-
"""모집단에서 빠진 «짧은 독립 문자열» 표.

★실기 2026-08-29: 「목화살**は** / 아스카에 맞았다!」 — 조사 「は」가 그대로 나왔다.
  `%sは` 는 NUL 로 끝나는 **4바이트 독립 문자열**이라 추출기가 걸러버렸다.
  TSV 모집단엔 「조사 잔여 0행」인데 화면엔 일본어가 남는 지문이 이것이다.
  ⇒ 「모집단이 맞는지는 실기 화면과 대조해야 안다」 [[feedback_verify_corpus_against_real_screen]]

빌더(`build_all.py` 6.6 단계)가 «현재 빌드의 글리프 배정표»로 인코딩해 제자리 치환한다.
길이가 같아야 오프셋이 안 밀린다 — 다르면 빌더가 건너뛰고 경고한다.

    (파일, 원문(NUL 제외), 번역)
"""

RULES = [
    ('SSCR.BIN',    '%sは', '%s은'),
    ('DATASCR.BIN', '%sは', '%s은'),
]
